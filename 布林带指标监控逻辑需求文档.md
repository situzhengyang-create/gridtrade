# 布林带指标监控系统需求文档

## 1. 概述

### 1.1 项目背景
为准确识别布林带挤压信号，本系统采用双重标准确认机制，结合经验法则和统计分位法，提高信号可靠性。系统通过监控布林带宽度变化，识别低波动率状态，为后续突破行情提供预警。

### 1.2 核心目标
- 准确识别布林带挤压信号
- 结合经验法则与统计方法，提高信号可靠性
- 通过持续时间过滤减少假信号
- 提供多维度挤压信号评估
- 为交易决策提供量化依据

## 2. 系统设计原理

### 2.1 布林带宽度计算
**布林带宽度** = (上轨 - 下轨) / 中轨

其中：
- 上轨 = 中轨 + 2 × 标准差
- 下轨 = 中轨 - 2 × 标准差
- 中轨 = 20日简单移动平均线
- 标准差 = 20日收盘价的标准差

### 2.2 双重标准确认机制
系统采用两层过滤机制确认挤压信号：

1. **第一层：经验法则触发**
   - 当前宽度 < 历史最低宽度 × 1.05
   - 快速捕捉宽度新低时刻

2. **第二层：统计分位确认**
   - 当前宽度百分位 < 20%
   - 确保宽度处于历史低位区间

3. **第三层：持续时间过滤**
   - 挤压状态持续至少5个交易日
   - 过滤短期波动造成的假信号

## 3. 参数固定值

| 参数名称 | 固定值 | 说明 |
|---------|-------|------|
| 布林带周期 | 20日 | 中轨计算周期 |
| 标准差倍数 | 2倍 | 上下轨计算参数 |
| 历史窗口周期 | 120个交易日 | 计算历史百分位的窗口 |
| 经验法则容错系数 | 1.05 | 历史最低宽度的放宽系数 |
| 统计分位阈值 | 20% | 低百分位阈值 |
| 最小持续时间 | 5个交易日 | 挤压信号最小持续天数 |

## 4. 数据处理逻辑

### 4.1 数据输入要求
- **输入数据**: 按时间顺序排列的每日行情数据
- **必需字段**: 日期、收盘价
- **数据排序**: 最早的数据在前，最新的数据在最后
- **最小长度**: 至少包含140个交易日数据（120日窗口+20日布林带计算）

### 4.2 计算步骤

#### 步骤1：计算布林带指标
对于每个交易日t：
1. 计算20日简单移动平均（中轨）
   ```
   Middle_Band[t] = SMA(Close[t-19:t], 20)
   ```
2. 计算20日标准差
   ```
   StdDev[t] = STDEV(Close[t-19:t], 20)
   ```
3. 计算上轨和下轨
   ```
   Upper_Band[t] = Middle_Band[t] + 2 × StdDev[t]
   Lower_Band[t] = Middle_Band[t] - 2 × StdDev[t]
   ```
4. 计算标准化宽度
   ```
   Width[t] = (Upper_Band[t] - Lower_Band[t]) / Middle_Band[t]
   ```

#### 步骤2：计算历史参考值
对于每个交易日t（需要至少120日历史数据）：
1. 获取历史宽度序列
   ```
   Historical_Widths = Width[t-119:t]  # 最近120个交易日宽度
   ```
2. 计算历史最低宽度（排除当日）
   ```
   Historical_Min = min(Width[t-119:t-1])  # 排除当日的前119日最小值
   ```
3. 计算当前宽度百分位
   ```
   # 在Historical_Widths中，找出小于等于当前宽度Width[t]的数量
   count_less_equal = sum(Historical_Widths <= Width[t])
   # 计算百分位
   Percentile_Rank = (count_less_equal - 1) / (len(Historical_Widths) - 1)
   ```

#### 步骤3：应用双重标准
对于每个交易日t：
1. **经验法则判断**
   ```
   Experience_Rule = Width[t] < Historical_Min × 1.05
   ```
2. **统计分位判断**
   ```
   Percentile_Rule = Percentile_Rank < 0.20
   ```
3. **双重标准同时满足**
   ```
   Squeeze_Signal = Experience_Rule AND Percentile_Rule
   ```

#### 步骤4：持续时间过滤
1. 记录Squeeze_Signal连续为True的天数
2. 当连续天数 ≥ 5时，确认为有效挤压信号
   ```
   if Squeeze_Signal[t] == True:
       Consecutive_Days[t] = Consecutive_Days[t-1] + 1
   else:
       Consecutive_Days[t] = 0
   
   Valid_Squeeze[t] = (Consecutive_Days[t] >= 5)
   ```

## 5. 信号分类与定义

### 5.1 信号等级定义
系统输出三种挤压信号等级：

| 信号等级 | 条件 | 含义 |
|---------|------|------|
| 初级挤压 | 经验法则或统计分位单一满足 | 潜在挤压状态，需观察 |
| 中级挤压 | 双重标准同时满足 | 确认挤压状态，关注突破 |
| 高强度挤压 | 双重标准满足且持续≥5日 | 强烈挤压信号，突破概率高 |

### 5.2 信号状态流转
```
无挤压状态
     ↓ (满足单一条件)
初级挤压状态
     ↓ (同时满足双重标准)
中级挤压状态
     ↓ (持续5日以上)
高强度挤压状态
     ↓ (任一条件不满足)
无挤压状态
```

## 6. 输出规范

### 6.1 完整输出结构
```json
{
  "date": "2026-05-10",
  "bollinger_data": {
    "upper_band": 105.32,
    "middle_band": 100.15,
    "lower_band": 94.98,
    "width": 0.1032,
    "width_percent": 10.32
  },
  "squeeze_analysis": {
    "historical_min_width": 0.0985,
    "current_percentile": 0.15,
    "experience_rule_met": true,
    "percentile_rule_met": true,
    "consecutive_days": 8,
    "dual_criteria_met": true
  },
  "squeeze_signal": {
    "signal_level": "HIGH_INTENSITY",
    "signal_name": "高强度挤压确认",
    "description": "波动率已降至极低水平，双重标准确认且持续8个交易日，突破概率显著增加"
  },
  "indicators": {
    "experience_condition": "Width(0.1032) < Historical_Min(0.0985) × 1.05 = 0.1034",
    "percentile_condition": "Percentile(15%) < 20%"
  }
}
```

### 6.2 字段说明
| 字段组 | 字段名 | 类型 | 说明 |
|--------|--------|------|------|
| 基本信息 | date | string | 分析日期 |
| 布林带数据 | upper_band | float | 布林带上轨值 |
|  | middle_band | float | 布林带中轨值 |
|  | lower_band | float | 布林带下轨值 |
|  | width | float | 标准化宽度（小数形式） |
|  | width_percent | float | 宽度百分比（×100） |
| 挤压分析 | historical_min_width | float | 历史最低宽度（120日） |
|  | current_percentile | float | 当前宽度百分位（0-1） |
|  | experience_rule_met | boolean | 经验法则是否满足 |
|  | percentile_rule_met | boolean | 统计分位是否满足 |
|  | consecutive_days | integer | 双重标准连续满足天数 |
|  | dual_criteria_met | boolean | 双重标准是否同时满足 |
| 信号输出 | signal_level | string | 信号等级 |
|  | signal_name | string | 信号名称 |
|  | description | string | 信号描述 |
| 指标显示 | experience_condition | string | 经验法则条件显示 |
|  | percentile_condition | string | 统计分位条件显示 |

### 6.3 信号等级映射
| 信号等级 | 条件组合 | 信号名称 | 颜色编码 | 优先级 |
|----------|----------|----------|----------|--------|
| 无挤压 | 双重标准均不满足 | 无挤压信号 | #808080 | 0 |
| 初级挤压 | 仅满足单一标准 | 初级挤压预警 | #FFC000 | 1 |
| 中级挤压 | 双重标准同时满足 | 中级挤压确认 | #FF9900 | 2 |
| 高强度挤压 | 双重标准满足且持续≥5日 | 高强度挤压确认 | #FF0000 | 3 |

## 7. 详细判断逻辑

### 7.1 完整判断流程
```
输入：当前交易日数据，历史宽度数据
输出：挤压信号等级

步骤：
1. 计算当前布林带宽度Width[t]
2. 计算历史最低宽度（前119日）Historical_Min
3. 计算当前宽度百分位Percentile_Rank
4. 判断经验法则：Exp_Rule = (Width[t] < Historical_Min × 1.05)
5. 判断统计分位：Per_Rule = (Percentile_Rank < 0.20)
6. 判断双重标准：Dual_Met = Exp_Rule AND Per_Rule

if Dual_Met:
    更新连续天数：Consecutive_Days = 前一日连续天数 + 1
else:
    重置连续天数：Consecutive_Days = 0

# 确定信号等级
if Consecutive_Days >= 5:
    signal_level = "HIGH_INTENSITY"
    signal_name = "高强度挤压确认"
    description = f"波动率已降至极低水平，双重标准确认且持续{Consecutive_Days}个交易日，突破概率显著增加"
elif Dual_Met:
    signal_level = "MEDIUM"
    signal_name = "中级挤压确认"
    description = "双重标准确认，波动率处于历史低位，关注潜在突破"
elif Exp_Rule OR Per_Rule:
    signal_level = "LOW"
    signal_name = "初级挤压预警"
    if Exp_Rule AND NOT Per_Rule:
        description = "经验法则触发，但统计分位未确认"
    elif NOT Exp_Rule AND Per_Rule:
        description = "统计分位确认，但未达到经验法则阈值"
    else:
        description = "单一标准满足，需进一步观察"
else:
    signal_level = "NONE"
    signal_name = "无挤压信号"
    description = "波动率处于正常水平"

返回：signal_level, signal_name, description, 及相关指标
```

### 7.2 特殊情况处理

#### 7.2.1 数据不足
- 当数据点 < 140时，无法进行完整计算
- 输出错误信息："数据不足，需要至少140个交易日数据"
- 部分计算可逐步启用：
  - 数据点 ≥ 20：可计算布林带
  - 数据点 ≥ 120：可计算百分位
  - 数据点 ≥ 140：可进行完整分析

#### 7.2.2 极值处理
- 当历史宽度标准差极小（<0.0001）时，可能表示价格长期横盘
- 此时挤压信号可能失效，需额外标注
- 输出备注："价格长期横盘，挤压信号参考价值降低"

#### 7.2.3 边界情况
- 当Width[t] = Historical_Min × 1.05时，判定为不满足经验法则
- 当Percentile_Rank = 0.20时，判定为不满足统计分位
- 使用严格小于判断，避免边界模糊

## 8. 计算示例

### 8.1 示例1：高强度挤压确认
**背景数据**：
- 当前日期：2026-05-10
- 当前宽度：0.025（2.5%）
- 历史最低宽度：0.0238（2.38%）
- 当前宽度百分位：0.15（15%）
- 双重标准已连续满足：8天

**计算过程**：
1. 经验法则判断：
   ```
   0.025 < 0.0238 × 1.05 = 0.02499
   0.025 < 0.02499 → False
   ```
   注：实际上0.025 > 0.02499，不满足严格小于条件
   
   修正：假设当前宽度为0.0245
   ```
   0.0245 < 0.0238 × 1.05 = 0.02499
   0.0245 < 0.02499 → True
   ```

2. 统计分位判断：
   ```
   0.15 < 0.20 → True
   ```

3. 双重标准同时满足：
   ```
   True AND True → True
   ```

4. 持续时间判断：
   ```
   连续满足天数 = 8 ≥ 5 → True
   ```

**输出结果**：
```json
{
  "date": "2026-05-10",
  "bollinger_data": {
    "width": 0.0245,
    "width_percent": 2.45
  },
  "squeeze_analysis": {
    "historical_min_width": 0.0238,
    "current_percentile": 0.15,
    "experience_rule_met": true,
    "percentile_rule_met": true,
    "consecutive_days": 8,
    "dual_criteria_met": true
  },
  "squeeze_signal": {
    "signal_level": "HIGH_INTENSITY",
    "signal_name": "高强度挤压确认",
    "description": "波动率已降至极低水平，双重标准确认且持续8个交易日，突破概率显著增加"
  }
}
```

### 8.2 示例2：初级挤压预警
**背景数据**：
- 当前宽度：0.026
- 历史最低宽度：0.025
- 当前宽度百分位：0.18
- 双重标准连续满足：0天

**计算过程**：
1. 经验法则：
   ```
   0.026 < 0.025 × 1.05 = 0.02625
   0.026 < 0.02625 → True
   ```

2. 统计分位：
   ```
   0.18 < 0.20 → True
   ```
   注：此例中双重标准均满足，应为中级挤压
   
   修正示例：假设百分位为0.22
   ```
   0.22 < 0.20 → False
   ```

3. 双重标准：
   ```
   True AND False → False
   ```

4. 但满足单一标准（经验法则）

**输出结果**：
```json
{
  "squeeze_signal": {
    "signal_level": "LOW",
    "signal_name": "初级挤压预警",
    "description": "经验法则触发，但统计分位未确认（当前百分位22%）"
  }
}
```

## 9. 监控与报警机制

### 9.1 信号状态监控
系统应监控以下状态变化：
1. **信号升级**：从无挤压→初级挤压→中级挤压→高强度挤压
2. **信号降级**：从高强度挤压→信号消失
3. **持续时间更新**：每日更新连续满足天数

### 9.2 报警触发条件
建议在以下情况触发报警：
1. 首次出现高强度挤压信号
2. 挤压信号持续超过10个交易日
3. 挤压信号消失（可能预示突破）

### 9.3 历史信号分析
系统应记录历史信号，用于：
1. 回测信号有效性
2. 分析信号持续时间分布
3. 优化参数设置（如需）

## 10. 参数选择说明

### 10.1 120日历史窗口
- 覆盖约6个月交易数据，包含足够市场周期
- 平衡统计显著性与计算效率
- 避免过长窗口导致信号迟钝

### 10.2 20%分位阈值
- 行业常用标准，表示宽度处于历史最低20%区间
- 统计意义上属于显著低波动状态
- 平衡信号灵敏度与可靠性

### 10.3 5日最小持续时间
- 过滤短期波动造成的假信号
- 确保挤压状态具有持续性
- 与价格形态形成时间相匹配

### 10.4 1.05容错系数
- 给予5%的容错空间，避免对极端值过于敏感
- 经验值，在实战中表现稳健
- 与统计分位法形成互补

## 11. 使用建议

### 11.1 交易应用
1. **高强度挤压信号**：
   - 准备突破策略
   - 设置突破预警
   - 控制仓位，准备跟随突破方向

2. **中级挤压信号**：
   - 密切关注价格行为
   - 结合其他指标确认
   - 避免过早进场

3. **初级挤压信号**：
   - 加入观察列表
   - 等待进一步确认
   - 不基于单一信号交易

### 11.2 风险管理
1. 挤压信号不是突破信号，只是突破的前奏
2. 需等待实际突破确认后再行动
3. 设置合理的止损位
4. 结合成交量等其他指标验证

### 11.3 组合使用
1. 与20日均线趋势系统结合使用
2. 当均线趋势明确且出现挤压信号时，信号更强
3. 注意不同时间框架的协调

## 12. 异常处理

### 12.1 数据异常
```json
{
  "error": "INSUFFICIENT_DATA",
  "message": "数据不足，需要至少140个交易日数据",
  "required": 140,
  "provided": 100
}
```

### 12.2 计算异常
```json
{
  "error": "CALCULATION_ERROR",
  "message": "布林带宽度计算失败",
  "details": "标准差计算异常"
}
```

### 12.3 信号异常
```json
{
  "warning": "UNUSUAL_SIGNAL",
  "message": "挤压信号持续时间异常",
  "details": "信号已持续20个交易日，建议检查数据或市场状况"
}
```

## 13. 系统输出示例

### 13.1 无挤压状态
```
日期：2026-05-10
布林带宽度：4.2%（历史分位：45%）
信号状态：无挤压信号
说明：波动率处于正常水平
```

### 13.2 初级挤压预警
```
日期：2026-05-10
布林带宽度：2.6%（历史分位：22%）
信号状态：初级挤压预警
说明：经验法则触发，但统计分位未确认
```

### 13.3 中级挤压确认
```
日期：2026-05-10
布林带宽度：2.4%（历史分位：15%）
信号状态：中级挤压确认
说明：双重标准确认，波动率处于历史低位
```

### 13.4 高强度挤压确认
```
日期：2026-05-10
布林带宽度：2.3%（历史分位：12%）
信号状态：高强度挤压确认
说明：双重标准确认且持续8个交易日，突破概率显著增加
```

---
**文档版本**：v1.0  
**创建日期**：2026年5月10日  
**最后更新**：2026年5月10日  
**状态**：已完成