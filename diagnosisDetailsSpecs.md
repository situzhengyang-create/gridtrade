# GridTrade 诊断报告详情页 (详情视图) 开发规格说明书

本说明书详细描述了 GridTrade 证券网格交易诊断报告详情视图的核心逻辑、数据传递、评分算法可视化及用户交互实现细节。

## 1. 组件接口定义 (`src/components/GridDiagnosisReport.tsx`)

详情视图的核心组件为 `GridDiagnosisReport`，它负责接收诊断数据并渲染交互式报告。

```typescript
interface Props {
  reports: DiagnosisReport[]; // 报告数组 (对应1年, 2年, 3年)
  symbol: string;           // 证券代码
  name: string;             // 证券名称
  isLoading?: boolean;      // 加载状态
  onApplySuggestion?: (min: number, max: number, step: number) => void; // 回调：应用建议参数
}
```

## 2. 状态管理与时间轴切换

组件通过内部状态 `activeTimeframe` 管理用户查看的诊断周期 (1年/2年/3年)。

```typescript
// -1: 对比模式, 0: 1年, 1: 2年, 2: 3年
const [activeTimeframe, setActiveTimeframe] = useState(-1); 

// 计算当前报告
const report = activeTimeframe === -1 ? null : reports[activeTimeframe];
```

## 3. 核心统计指标与可视化逻辑

### 3.1 指标卡片 `MetricCard` 全量细节

详情页中的所有性能指标均封装为 `MetricCard`。下表列出了所有指标的显示细节、算法定义及评估逻辑，以便开发者能够完整复刻报告系统的“灵魂”。

| 指标名称 (Label) | 包含子项 (Items) / 值 | 算法定义 (Definition) | 观测标准 (Standards) | 投资意义 (Significance) | 评估逻辑 (Evaluation) |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **A. 趋势强度** | 累计涨幅, 连涨跌 | 1. 累计收益率 = (最新收盘价 - 最早收盘价) / 最早收盘价 × 100%<br>2. 年化收益率 = 累计收益率 × (252 / 交易日数) | • ±10%内 (+1分)<br>• 连涨跌≤3天 (+2分)<br>• 连涨跌≤5天 (+1分) | 反映总收益与期限对比。震荡市 (±10%) 最适合网格。 | 累计收益率 > 10% 意味着单边趋势，易踏空或深套。 |
| **B. 波动水平** | 年化波动, 均振幅 | 1. 年化波动率 = 日收益率标准差 × √252 × 100% | • 20%-40% (+3分)<br>• 15-20% 或 40-50% (+2分)<br>• >50% (+1分) | 波动是盈利来源；太小无机会，太大风险高。 | 20-40% 适中；<20% 难以触发套利；>40% 风险高。 |
| **C. 震荡频率** | 交替频率 | 1. 涨跌交替频率 = 方向变化次数 / 总天数 × 100%<br>2. 统计最大连续上涨/下跌天数 | • 交替频率≥50% (+2分)<br>• 交替频率≥40% (+1分) | 涨跌交替频繁才能高抛低吸；连跌易致资金站岗。 | 频率高可发挥低买高卖优势。 |
| **D. 价格分布** | 通道占比 | 1. 计算20日布林带 (中轨 ± 2×标准差)<br>2. 占比 = 落在轨道内天数 / 总天数 × 100% | • 占比≥90% (+2分)<br>• 占比≥80% (+1分) | 价格在通道内运行是震荡特征；突破说明有趋势。 | 比例越高说明均值回归特性越强。 |

*   **UI 行为**: 以上指标点击 `svg` 按钮会展示上述“算法定义”。
*   **多维度展示**: 例如“连涨跌”显示为 `maxUp / maxDown` 天数，使用了 `item.items` 数组渲染。
*   **观测标准动态标记**: 如果当前标的符合某项标准，UI 会渲染 `✓` 绿色标记并高亮显示加分。

### 3.2 历史数据回顾区 (黄色卡片)

该区域展示了更直观的实操指标：

1.  **平均振幅 / 中位数振幅**:
    *   计算：`((当日高 - 当日低) / 前收盘) * 100` 的平均值或中位数。
    *   意义：反映日内波动典型水平。
2.  **建议网格大小**:
    *   计算：`中位数振幅 * 1.2` (向上取整保留2位，最小0.5%)。
3.  **价格运行区间的可视化逻辑**:
    *   **容器高度**: 固定高度的比例尺条。
    *   **建议覆盖圈**: `[safeGridMin, safeGridMax]` (底层逻辑通常取价格序列的 5% 和 95% 分位数)。
    *   **当前价标记**: `currentPrice` 在 `[historicalMin, historicalMax]` 范围内的相对百分比位置。

### 3.2 评分等级颜色映射 (`getRatingColor`)
根据结论状态动态应用 Tailwind CSS 类名：
```typescript
const getRatingColor = (rating: string) => {
  switch (rating) {
    case '非常适合': return 'text-emerald-700 bg-emerald-50 border-emerald-200';
    case '适合': return 'text-blue-700 bg-blue-50 border-blue-200';
    case '勉强适合': return 'text-amber-700 bg-amber-50 border-amber-200';
    default: return 'text-rose-700 bg-rose-50 border-rose-200';
  }
};
```

## 4. 可视化交互：价格运行区间

详情页核心交互部分在于动态渲染历史价格区间与建议覆盖范围。

```typescript
// HTML 结构逻辑细节：
// 1. 使用相对定位的容器：.relative 
// 2. 建议覆盖区间条：通过计算 safeMin, safeMax 在 historicalMin/Max 的百分比位置
// 3. 当前价格指示器：计算当前价格百分比，渲染一个圆形标志
// 这些均基于 report.backtest 中的极值数据在渲染时实时计算坐标。
```

## 5. UI 交互：跳转逻辑 (`handleStockClick`)

点击证券代码时，根据设备类型智能跳转。

```typescript
const handleStockClick = (e: React.MouseEvent, sym: string) => {
  const isMobile = /iPhone|iPad|iPod|Android/i.test(navigator.userAgent);
  
  if (isMobile) {
    // 优先尝试 scheme，超时后打开wap版
    window.location.href = getEastMoneyAppScheme(sym);
    setTimeout(() => {
      if (!document.hidden) window.open(getEastMoneyWapUrl(sym), '_blank');
    }, 2500);
  } else {
    // 桌面端直接打开 web 版
    window.open(getEastMoneyUrl(sym), '_blank');
  }
};
```

## 6. 核心数据解析与评分详析

详情视图末尾是 `核心性能指标详析`，将评分体系拆解为四个维度，每个维度的计算逻辑如下：

### A. 趋势强度 (满分 3分)
*   **物理意义**: 判断证券是否处于单边行情中，还是震荡行情中。
*   **计算指标**:
    *   `累计涨幅`: `((最新收盘价 - 最早收盘价) / 最早收盘价) * 100%`
    *   `连涨天数`: 连续收盘价 `change_pct > 0` 的天数。
*   **评分逻辑**:
    *   `累计收益率` 绝对值 `≤ 10%` (+1分) -> 震荡市特征。
    *   `最大连续涨跌幅天数` `≤ 3天` (+2分) -> 典型的震荡市，无明显趋势。
    *   `最大连续涨跌幅天数` `≤ 5天` (+1分) -> 可能存在短期单边趋势。

### B. 波动水平 (满分 3分)
*   **物理意义**: 衡量网格套利空间。波动太小没利润，波动太大风险高。
*   **计算指标**:
    *   `年化波动率`: 日收益率标准差 × `√252` × `100%`。
*   **评分逻辑**:
    *   `20% ≤ 年化波动率 ≤ 40%` (+3分) -> 最优区间。
    *   `15% ≤ 波动 < 20%` 或 `40% < 波动 ≤ 50%` (+2分) -> 偏低或偏高。
    *   `波动率 > 50%` (+1分) -> 高风险。

### C. 震荡频率 (满分 2分)
*   **物理意义**: 多久价格会反转，保证网格能高抛低吸。
*   **计算指标**:
    *   `涨跌交替频率`: `方向变化次数 / (总交易日数 - 1) * 100%`。
*   **评分逻辑**:
    *   `交替频率 ≥ 50%` (+2分) -> 震荡特征明显，交易机会多。
    *   `交替频率 ≥ 40%` (+1分) -> 有一定震荡频率。

### D. 价格分布 (满分 2分)
*   **物理意义**: 均值回归特性。反映价格是否倾向于回到布林带均值。
*   **计算指标**:
    *   使用 20 日滑动窗口计算布林带上下轨（`中轨 ± 2×标准差`）。
    *   `布林带占比`: 落在轨道内的交易日天数 / 总天数 × 100%。
*   **评分逻辑**:
    *   `占比 ≥ 90%` (+2分) -> 均值回归特性极强。
    *   `占比 ≥ 80%` (+1分) -> 回归特性良好。

## 7. 界面 UI 文案索引 (Copywriting)

以下为诊断报告详情页中使用的关键 UI 文案，方便进行本地化或复刻开发：

### 7.1 周期选择器
*   `对比模式`: 用于展示各周期对比表格。
*   `最近1年`, `最近2年`, `最近3年`: 具体的诊断周期切换。

### 7.2 优势与风险文案库
诊断报告会根据指标自动命中并展示以下文案：

**优势指标 (Advantages)**:
*   `波动率处于网格交易的最优区间`
*   `震荡市趋势特征明显`
*   `中短期无明显趋势，震荡频繁`
*   `价格震荡频繁，提供了充足的交易机会`
*   `价格主要运行在布林带通道内`

**风险预警 (Risks)**:
*   `波动率稍显过低或过高，可能影响网格效率`
*   `波动率风险较高`
*   `波动率过低，可能影响网格效率`
*   `存在一定的单边趋势可能`
*   `存在潜在单边趋势，需警惕网格被套或踏空`
*   `震荡不够频繁，可能导致网格交易冷清`
*   `价格常出现布林带通道突破，风险较高`
*   `暂无明显重大风险指标` (缺省时)

### 7.3 状态提示与通用文案
*   **加载中**: `正在生成诊断报告...` / `通常需要几秒钟，请稍候`
*   **空状态**: `暂无诊断报告` / `该标的历史数据不足，无法生成有效诊断`
*   **评分总结**: `评估维度与权重（总分10分）`
*   **回测时间轴**: `历史数据回顾 ({timeframe})` / `更新于 {time}`
*   **操作按钮**: `一键预置网格参数`, `查看定义与算法`, `查看评判逻辑`

### 7.3 表格与卡片头部
*   `评估维度`: 对比表格类目列名。
*   `总计`: 评分总计行。
*   `适合程度`: 评分评级行。
*   `建议`: 策略建议概括行。
*   `核心性能指标详析`: 指标分析区域标题。
*   `观测标准`: 行业标准详情。
*   `投资意义`: 指标投资逻辑。

### 7.4 维度与指标名称
*   `趋势强度` (`(3分)`): 维度名称。指标包括: `累计涨幅`, `连涨跌`。
*   `波动水平` (`(3分)`): 维度名称。指标包括: `年化波动`, `均振幅`。
*   `震荡特征` (`(2分)`): 维度名称。指标包括: `交替频率`。
*   `价格分布` (`(2分)`): 维度名称。指标包括: `通道占比`。

### 7.5 交互控件
*   `查看定义与算法`: (Tooltip) 指标定义按钮。
*   `一键预置网格参数`: (按钮) 应用建议参数至配置页。

